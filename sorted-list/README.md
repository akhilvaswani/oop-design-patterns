# Sorted Integer List

Extends an IntList base class to keep elements sorted on insert.

The `add()` method finds the correct insertion index, shifts existing elements to the right, then places the new value. Basically insertion sort built into the data structure.

*Note: requires IntList base class (provided by instructor).*

### Concepts
Inheritance, method overriding, insertion sort, array shifting
